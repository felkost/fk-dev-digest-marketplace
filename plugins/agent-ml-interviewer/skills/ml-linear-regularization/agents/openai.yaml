interface:
  display_name: "Лінійні моделі й регуляризація"
  short_description: "VIF-драбина, L1/L2 і 1-SE, впливові рядки (плече/DFBETA/Cook)"
  default_prompt: Use $ml-linear-regularization to audit multicollinearity with VIF, pick L1/L2 regularisation with log-scale grids and the one-standard-error rule, and find rows that drive the fit via leverage, DFBETA and Cook; answer in Ukrainian.
policy:
  products:
    - chatgpt
    - codex
    - api
    - atlas
  allow_implicit_invocation: true
