interface:
  display_name: "Навчання нейромереж"
  short_description: "η за порогом, епохи через раннє спин, батч за часом"
  default_prompt: Use $nn-training-params to set neural network training hyperparameters — learning rate threshold procedure, early stopping, batch size, regularization; answer in Ukrainian.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
