# Pin exact versions at build time -- look them up when generating the
# example, do not trust remembered version numbers.
#
# harness_core.py imports none of this on purpose: the evaluator, the
# grounding check, N-repeat benchmarking, defect localization, the
# minimum-change ladder and the retry-ceiling policy are testable with a
# bare interpreter (see tests/smoke_test.py in the plugin root).

langchain-openai
python-dotenv     # loads .env into os.environ, used only by agent.py
