# Pin exact versions at build time -- look them up when generating the
# example, do not trust remembered version numbers.
#
# loop_core.py imports none of this on purpose: the layered stop gate, the
# de-duplicated queue and the offload log are testable with a bare
# interpreter (see tests/smoke_test.py in the plugin root).

langchain-openai
python-dotenv     # loads .env into os.environ, used only by agent.py
