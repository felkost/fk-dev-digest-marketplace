---
description: Wrap up this session using the engineering-insights skill — capture non-obvious discoveries into the affected module's insights.md.
---

Wrap up this session using the `sdd-engineering:engineering-insights` skill.

Steps:
1. Identify which module of the host repository was most directly worked on in this session (a top-level package/workspace, or the repo root for cross-cutting work).
2. Read that module's `insights.md`. Output a summary: total entries found, and any entries relevant to what was worked on today.
3. Load the `sdd-engineering:engineering-insights` skill and work through its wrap-up checklist (Mode B).
4. For each candidate insight, apply the four-test discovery bar from the skill's `criteria.md`.
5. Write passing entries using the format in the skill's `format.md`. Append only — never rewrite existing entries.
6. Update the `Last updated` footer.
7. Report what was added, or confirm zero entries if the session had no qualifying discoveries.
